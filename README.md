# Bitcoin BLockchain like OP_RETURN scanner, in work

clone -> npm install -> change credentials in src/opscanner -> npm run dev

You need to run a local blockchain or may connect to remote. Remote is untested.
